services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
      - ./content:/app/content
      - ./public:/app/public
    environment:
      - NODE_ENV=development
    command: npm run dev -- -H 0.0.0.0