---
title: "Dockerfile Basics"
module: docker
level: beginner
duration: "35 min"
diagram: docker-image-layers
description: "Learn to write Dockerfiles, understand layer caching, and build custom images."
order: 2
---

# Writing Your First Dockerfile

A Dockerfile is a text file containing instructions to build a Docker image. Each instruction creates a layer in the image.

## Basic Structure

```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 3000
CMD ["node", "server.js"]
```

## Key Instructions

- `FROM` — Base image
- `WORKDIR` — Set working directory
- `COPY` — Copy files from host to image
- `RUN` — Execute commands during build
- `EXPOSE` — Document which port the container listens on
- `CMD` — Default command when container starts

## Layer Caching

Docker caches each layer. If a layer hasn't changed, Docker reuses the cache instead of rebuilding. Order your instructions from least to most frequently changing:

1. System dependencies (rarely change)
2. Package installs (change with dependency updates)
3. Application code (changes most often)

## .dockerignore

Create a `.dockerignore` file to exclude files from the build context:

```
node_modules
.git
.env
Dockerfile
```

## Try It Yourself

Use the terminal below to practice building and inspecting images.